apiVersion: apps/v1
kind: Deployment
metadata:
  name: opslevel-worker-faktory
  labels:
    app.kubernetes.io/component: worker-faktory
    app.kubernetes.io/part-of: opslevel
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 75%
  selector:
    matchLabels:
      app.kuberntes.io/component: worker-faktory
      app.kubernetes.io/part-of: opslevel
  template:
    metadata:
      labels:
        app.kuberntes.io/component: worker-faktory
        app.kubernetes.io/part-of: opslevel
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/opslevel/configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/opslevel/secret.yaml") . | sha256sum }}
{{- with .Values.opslevel.pod.annotations }}
{{ toYaml . | indent 8 }}
{{- end }}
    spec:
      {{- template "opslevel.pullSecrets" . }}
      {{- template "global.nodeSelector" . }}
      priorityClassName: {{ .Values.priorityClasses.normal }}
      {{- with .Values.opslevel.securityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      terminationGracePeriodSeconds: 300
      {{- if .Values.certificate.enabled }}
      initContainers:
        - name: init-certs
          image: "{{ template "opslevel.image" . }}"
          imagePullPolicy: IfNotPresent
          command:
            - sh
            - -c
            - sleep 10; update-ca-certificates; cp -r /etc/ssl/certs/. /data;
          volumeMounts:
            - name: certificate
              mountPath: /usr/local/share/ca-certificates/custom-cert.crt
              subPath: custom-cert.crt
              readOnly: false
            - name: ca
              mountPath: /data
              readOnly: false
      {{- end }}
      containers:
        - name: worker-faktory
          image: "{{ template "opslevel.image" . }}"
          imagePullPolicy: IfNotPresent
          args:
            - ./bin/run
            - faktory-worker
            - -C config/faktory.yml
          envFrom:
            - configMapRef:
                name: 'opslevel'
            - secretRef:
                name: 'opslevel'
            - secretRef:
                name: '{{ .Values.redis.secret.name }}'
            {{- template "opslevel.integration.secrets" . }}
          {{- with .Values.opslevel.workerLow.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.opslevel.containerSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
          {{- include "opslevel.writableVolumeMounts" . | nindent 12 }}
          {{- if .Values.certificate.enabled }}
            - name: certificate
              mountPath: /usr/local/share/ca-certificates/custom-cert.crt
              subPath: custom-cert.crt
              readOnly: false
            - name: ca
              mountPath: /etc/ssl/certs
          {{- end }}
      volumes:
      {{- include "opslevel.writableVolumes" . | nindent 8 }}
      {{- if .Values.certificate.enabled }}
        - name: ca
          emptyDir: {}
        - name: certificate
          secret:
            secretName: {{ .Values.certificate.secret.name }}
      {{- end }}
